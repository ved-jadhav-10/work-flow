# ============================================
# Workflow — Environment Variables
# ============================================
# Copy this file to .env in both client/ and server/ directories
# and fill in your actual values. NEVER commit .env files.

# ── Database (Neon PostgreSQL) ─────────────────
DATABASE_URL=postgresql://neondb_owner:your-password@ep-xxx-yyy.region.aws.neon.tech/neondb?sslmode=require

# ── Appwrite Cloud (File Storage) ─────────────
APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
APPWRITE_PROJECT_ID=your-appwrite-project-id
APPWRITE_API_KEY=your-appwrite-api-key
APPWRITE_BUCKET_ID=your-appwrite-bucket-id

# ── Google Gemini API (AI Studio) ─────────────
GEMINI_API_KEY=your-gemini-api-key

# ── Groq API (Fallback LLM) ──────────────────
GROQ_API_KEY=your-groq-api-key

# ── GitHub OAuth (NextAuth) ───────────────────
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# ── NextAuth.js ───────────────────────────────
NEXTAUTH_SECRET=generate-a-random-32-char-string
NEXTAUTH_URL=http://localhost:3000

# ── Backend ───────────────────────────────────
JWT_SECRET=generate-a-different-random-32-char-string
BACKEND_URL=http://localhost:8000

# ── Frontend (public, exposed to browser) ─────
NEXT_PUBLIC_API_URL=http://localhost:8000

# ── Ollama (Local Inference — optional) ───────
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=phi3:mini

# ── Deployment (set these in production) ──────
# NEXTAUTH_URL=https://your-app.vercel.app
# NEXT_PUBLIC_API_URL=https://your-api.onrender.com
# BACKEND_CORS_ORIGINS=https://your-app.vercel.app
